version: '3.8'

services:
  api:
    build: .
    container_name: loouwd-api
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=production
      - HOST=0.0.0.0
      - PORT=8000
      - CACHE_ENABLED=true
    restart: unless-stopped
